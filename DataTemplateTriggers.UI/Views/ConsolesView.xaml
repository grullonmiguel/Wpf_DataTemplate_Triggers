<UserControl
    x:Class="DataTemplateTriggers.UI.Views.ConsolesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DataTemplateTriggers.UI.ViewModels">

    <!--  Set DataContext for this View  -->
    <UserControl.DataContext>
        <local:ConsolesViewModel />
    </UserControl.DataContext>

    <Border Padding="24" Background="{StaticResource BackgroundBrush2}" CornerRadius="5">

        <Border.Effect>
            <DropShadowEffect ShadowDepth="3" BlurRadius="4" Direction="315" Opacity="0.7" />
        </Border.Effect>
        <Grid>

            <!--  Definitions  -->
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <!--  Header  -->
            <TextBlock Style="{StaticResource HeaderText}" Text="{Binding Header}" />

            <!--  Game Console List  -->
            <ListBox
                Grid.Row="1"
                Background="Transparent"
                BorderThickness="0" />

        </Grid>
    </Border>

</UserControl>