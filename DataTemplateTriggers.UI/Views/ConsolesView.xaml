<UserControl
    x:Class="DataTemplateTriggers.UI.Views.ConsolesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DataTemplateTriggers.UI.ViewModels">

    <!--  Set DataContext for this View  -->
    <UserControl.DataContext>
        <local:ConsolesViewModel />
    </UserControl.DataContext>

    <Border Style="{StaticResource Card}">
        <Grid>

            <!--  Definitions  -->
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <!--  Header  -->
            <TextBlock Style="{StaticResource HeaderText}" Text="{Binding Header}" />

            <!--  Game Console List  -->
            <ListBox
                Grid.Row="1"
                Margin="0,16,0,8"
                ItemsSource="{Binding Consoles}" />

        </Grid>
    </Border>

</UserControl>